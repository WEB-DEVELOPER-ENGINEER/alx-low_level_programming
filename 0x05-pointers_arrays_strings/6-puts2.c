#include "main.h"

/**
 * puts2 - prints every other character
 * @str: char parameter
 */

void puts2(char *str)
{
		int i;
		int j;

		for (i = 0; 0 < 1; i++)
		{
			if (s[i] == 0)
			break;
		}
		for (j = i; j >= 0; j -= 2)
		{
			if (s[j] != 0)
			_putchar(s[j]);
		}
		_putchar('\n');
}
