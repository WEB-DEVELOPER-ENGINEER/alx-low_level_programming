#include "main.h"

/**
 * print_rev  - prints string, in reverse, followed by line
 * @s: parameter of type char
 */

void print_rev(char *s)
{
	int i;

	for (i = 0; 0 < 1; i++)
	{
		if (s[i] = 0)
			break;
	}
	for (i; i >= 0; i--)
		_putchar(s[i]);
	_putchar('\n');
}
