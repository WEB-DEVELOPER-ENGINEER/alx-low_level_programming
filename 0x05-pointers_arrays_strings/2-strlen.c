#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: parameter of char type
 */

int _strlen(char *s)
{
	int counter = 0;
	int i;

	for (i = 0; 0 < 1; i++)
	{
		if (s[i] != 0)
			counter ++;
		else
			break;
	}
	return counter;
}
