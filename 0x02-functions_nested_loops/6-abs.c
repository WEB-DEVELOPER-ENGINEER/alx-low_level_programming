#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 *
 * @n - integer
 *
 * Return: 1 for lowercase. 0 for the rest.
 */

int _abs(int n)
{
	if (n < 0)
		n = n * -1;
	
	return (0);
}
